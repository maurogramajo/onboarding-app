import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  button: {
    borderRadius: 50,
    paddingHorizontal: 16,
    alignItems: 'center',
  },
});

export default styles;
