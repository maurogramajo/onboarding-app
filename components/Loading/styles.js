import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#0B1626',
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default styles;
